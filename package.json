{
  "name": "@lindorm-io/key-pair",
  "description": "ECC and RSA key-pair tools for lindorm.io packages",
  "version": "0.0.2",
  "license": "ISC",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "repository": {
    "type": "git",
    "url": "git@github.com:lindorm-io/key-pair.git"
  },
  "bugs": {
    "url": "https://github.com/lindorm-io/key-pair/issues"
  },
  "author": "Jonn Nilsson",
  "maintainers": [
    "jonn@jonn.nu"
  ],
  "scripts": {
    "build": "tsc",
    "clean": "concurrently 'rimraf coverage' 'rimraf dist'",
    "coverage": "jest --coverage",
    "lint": "eslint --ext .ts ./src/",
    "lint:fix": "eslint --ext .ts ./src/ --fix",
    "postversion": "git push --no-verify && git push --tags --no-verify",
    "prepublishOnly": "npm run clean && concurrently 'npm run build' 'npm run coverage'",
    "preversion": "npm run validate",
    "publish:patch": "npm version patch && npm publish",
    "publish:minor": "npm version minor && npm publish",
    "publish:major": "npm version major && npm publish",
    "test": "jest --config jest.config.js",
    "test:clear": "jest --clearCache",
    "test:watch": "jest --config jest.config.js --watch --",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "update": "ncu -a && npm install",
    "validate": "npm run test:clear && npm run lint && npm run typecheck && npm run test"
  },
  "dependencies": {
    "@lindorm-io/core": "^0.0.1",
    "date-fns": "^2.16.1",
    "lodash": "^4.17.20"
  },
  "peerDependencies": {},
  "devDependencies": {
    "@types/jest": "^26.0.15",
    "@types/jsonwebtoken": "^8.5.0",
    "@types/lodash": "^4.14.163",
    "@types/node": "^14.14.6",
    "@types/uuid": "^8.3.0",
    "@typescript-eslint/eslint-plugin": "^4.6.0",
    "@typescript-eslint/parser": "^4.6.0",
    "concurrently": "^5.3.0",
    "eslint": "^7.12.1",
    "husky": "^4.3.0",
    "jest": "^26.6.1",
    "jest-extended": "^0.11.5",
    "lint-staged": "^10.5.0",
    "mockdate": "^3.0.2",
    "prettier": "2.1.2",
    "rimraf": "^3.0.2",
    "ts-jest": "^26.4.3",
    "ts-node": "^9.0.0",
    "typescript": "^4.0.5"
  }
}
